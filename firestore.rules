rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Règles permissives pour le développement
    // À renforcer pour la production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
